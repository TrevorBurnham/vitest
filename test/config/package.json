{
  "name": "@vitest/test-config",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "/bin/sh -c \"echo 'Shell test' && pwd && ls | head -5 && vitest --typecheck.enabled\""
  },
  "devDependencies": {
    "@vitest/test-dep-conditions": "file:./deps/test-dep-conditions",
    "tinyexec": "^0.3.2",
    "vite": "latest",
    "vitest": "workspace:*"
  }
}
