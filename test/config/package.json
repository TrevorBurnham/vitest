{
  "name": "@vitest/test-config",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "echo '[DEBUG] test script starting' && echo '[DEBUG] PWD:' $PWD && echo '[DEBUG] NODE:' $(which node) && echo '[DEBUG] Node version:' && node --version && echo '[DEBUG] Running simple test:' && node simple-test.js && echo '[DEBUG] Simple test passed, running debug-test:' && node debug-test.js"
  },
  "devDependencies": {
    "@vitest/test-dep-conditions": "file:./deps/test-dep-conditions",
    "tinyexec": "^0.3.2",
    "vite": "latest",
    "vitest": "workspace:*"
  }
}
